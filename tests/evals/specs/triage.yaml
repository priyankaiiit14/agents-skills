skill: triage
input: |
  New issue submitted:

  "The export button on the reports page does nothing when clicked. Tested on Chrome and
  Firefox. Started after the v2.3 deploy yesterday."
rubric: |
  - The response assigns a category role of bug (something is broken, not a new feature).
  - The response assigns exactly one state role (needs-triage, needs-info, ready-for-agent, ready-for-human, or wontfix).
  - Any generated comment or response starts with the AI disclaimer line: > *This was generated by AI during triage.*
  - The response does not assign conflicting or multiple state roles.
